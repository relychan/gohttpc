# yaml-language-server: $schema=../jsonschema/gohttpc.schema.json
timeout: 60
transport: {}
authentication:
  tokenLocation:
    in: header
    name: x-hasura-admin-secret
    scheme: bearer
  type: http
  value:
    env: BEARER_TOKEN
retry:
  maxAttempts: 2
